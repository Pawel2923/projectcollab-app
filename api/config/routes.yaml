controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

auth:
    path: /auth/login
    methods: ['POST']

api_refresh_token:
    path: /auth/refresh
    methods: ['POST']
